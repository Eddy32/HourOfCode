var larguraJanela = 48;
var alturaJanela = 27;
var escala = 20;

function setup() {
	createCanvas(larguraJanela * escala, alturaJanela * escala);
	frameRate(25);
}

function draw(){
	background(51);
}
